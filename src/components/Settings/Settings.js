import React from 'react';
import style from './Settings.module.css';

const Settings = (props) => {
    return (
        <div className = {style.content}> SUP settings</div>
    );
}

export default Settings;